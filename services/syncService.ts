// Firestore sync disabled - stubs kept for interface compatibility

export const syncToFirestore = async (_email: string) => {
  return;
};

export const loadFromFirestore = async (_email: string) => {
  return false;
};
