name: Auto PR from dev

on:
  push:
    branches-ignore: [main]

jobs:
  create-pr:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write
    steps:
      - uses: actions/checkout@v4

      - name: Create or skip PR
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          BRANCH_NAME="${GITHUB_REF_NAME}"
          existing_pr=$(gh pr list --head "$BRANCH_NAME" --base main --state open --json number --jq '.[0].number')

          if [ -n "$existing_pr" ]; then
            echo "PR #${existing_pr} already exists — new commits are included automatically."
          else
            gh pr create \
              --head "$BRANCH_NAME" \
              --base main \
              --draft \
              --title "feat: changes from $BRANCH_NAME" \
              --body "Automated PR from $BRANCH_NAME branch. Mark as Ready for review when done to trigger CI/CD."
            echo "New PR created."
          fi
