name: Auto PR from dev

on:
  push:
    branches: [dev]

jobs:
  create-pr:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write
    steps:
      - uses: actions/checkout@v4

      - name: Create or skip PR
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          existing_pr=$(gh pr list --head dev --base main --state open --json number --jq '.[0].number')

          if [ -n "$existing_pr" ]; then
            echo "PR #${existing_pr} already exists — new commits are included automatically."
          else
            gh pr create \
              --head dev \
              --base main \
              --title "feat: changes from dev" \
              --body "Automated PR from dev branch. Review the commits below."
            echo "New PR created."
          fi
